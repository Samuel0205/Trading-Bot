# requirements.txt
alpaca-trade-api==3.2.0
flask==3.0.3
flask-socketio==5.3.6
pytz==2024.1
eventlet==0.36.1
requests==2.32.5

# ── railway.toml (save as a separate file named exactly: railway.toml) ──
# [build]
# builder = "NIXPACKS"
#
# [deploy]
# startCommand = "python bot.py"
# restartPolicyType = "ON_FAILURE"
# restartPolicyMaxRetries = 3
